product:
  type: jvm/app
  platforms: [ jvm ]


settings:
  springBoot:
    enabled: true
    version: 3.5.3
  jvm:
    release: 21
    mainClass: dev.amper.boot.Application

dependencies:
  - $spring.boot.starter.web

test-dependencies:
  - $spring.boot.starter.test